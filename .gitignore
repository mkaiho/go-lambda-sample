# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# env files
.envrc

# SSH Keys
.ssh/

# MySQL data
docker/mysql/data/

# localstack data
docker/localstack/data/
docker/localstack/.localstack
docker/localstack/server.test.pem
docker/localstack/server.test.pem.crt
docker/localstack/server.test.pem.key

# aws-cli
.aws/
